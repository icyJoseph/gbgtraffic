{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "backend.js",
  "scripts": { 
    "deploy": "wt create index.js --name bus-app --secrets-file .secrets",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "nodemon --exec \"wt serve backend.js --hostname localhost --port 1337 --secrets-file .secrets --meta ENV=DEV\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "qs": "^6.5.2",
    "uuid": "^3.3.2",
    "webtask-tools": "^3.4.0"
  }
}
